

const enum GenderEnum { // takes default values as 0,1
    Male,
    Female
}

const enum RoleEnum { // takes default values as 0,1
    Admin, 
    User,
}

const enum ProviderEnum {
    System,
    Google,
}

export {
    GenderEnum,
    RoleEnum,
    ProviderEnum,
}